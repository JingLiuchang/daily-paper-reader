---
title: "Parameterized Dense-Sparse Fusion for Hybrid Retrieval: Tuning a Rank-Score Mix on BEIR SciFact with Qdrant"
title_zh: 面向混合检索的参数化稠密-稀疏融合：在 BEIR SciFact 上使用 Qdrant 调优排序-分数混合
authors: "Satyanarayan Pati, Srikanth Patil"
date: 2026-09-19
pdf: "https://arxiv.org/pdf/2609.22770v1"
tags: ["query:vector-ann"]
score: 6.0
evidence: "在Qdrant向量数据库上进行稠密-稀疏混合检索并评估recall@10"
tldr: "稠密与稀疏混合检索常依赖人工融合权重，本文提出参数化 rank-score 混合排序器，用稠密先验α、分数-排名混合λ、RRF平滑κ和路由阈值τ等少量显式参数控制融合。作者在 BEIR SciFact 训练集（809查询）网格搜索，并冻结到测试集（300查询）。调优后 α=0.8、λ=0.75、κ=20 取得 0.753 nDCG@10 与 0.889 recall@10，优于稠密 BGE（0.742/0.871）和等权 RRF（0.707）。在20个索引单元独立复现中，该方法对等权 RRF 20/20 胜、对稠密16/20胜；系数具数据集特异性，其他语料应复用参数范围而非照搬 SciFact 点。"
source: arxiv
selection_source: fresh_fetch
motivation: 稠密与稀疏检索各有优势，但混合排序权重通常靠人工设定，缺少可解释、可迁移的参数化调优方案。
method: 提出参数化 rank-score 融合，含稠密先验α、分数-排名混合λ、RRF平滑κ、列表几何系数与路由阈值τ；在SciFact训练集网格搜索，测试集冻结评估。
result: "调优α=0.8、λ=0.75、κ=20 达0.753 nDCG@10、0.889 recall@10，超稠密BGE与等权RRF；20单元复现中胜RRF 20/20、胜稠密16/20。"
conclusion: 参数具数据集特异性；其他语料应复用搜索范围，而非照搬SciFact最优配置。
---

## 摘要
我们研究一种参数化混合排序器，它融合稠密嵌入列表与稀疏词法列表。该方法有一个小型的显式参数向量：稠密先验 $α ∈ [0,1]$、分数-排序混合 $λ ∈ [0,1]$、RRF 平滑参数 $κ > 0$、可选的列表几何系数（使 $α$ 随查询变化），以及一个可关闭稀疏检索的路由边际 $τ$。我们在 SciFact 训练集（809 个查询）上对这些范围进行网格搜索，并在 SciFact 测试集（300 个查询）上冻结所选值。调优后的排序-分数混合（$α= 0.8$，$λ= 0.75$，$κ= 20$）在该测试划分上达到 0.753 nDCG@10 和 0.889 recall@10，优于稠密 BGE（0.742 / 0.871）和等权 RRF（0.707 nDCG@10）。列表条件化的 $α$ 增加 +0.0006 nDCG；关闭稀疏检索的路由器被同一训练集拒绝（任何跳过约 50% 查询的 $τ$ 都会损失 nDCG）。这些系数是数据集特定的。使用相同模型的等权 RRF 在九组 BEIR 宏平均上未能优于稠密（0.479 对 0.519 nDCG@10）。在所有 20 个索引单元上独立重复相同的先训练后冻结扫描，在 20/20 上优于等权 RRF，在 16/20 上优于稠密（单元平均 nDCG@10：0.467 对稠密 0.462 对 RRF 0.420）。其他语料库应复用这些范围，而不是复制 SciFact 的那个点。

## Abstract
We study a parameterized hybrid ranker that fuses a dense embedding list and a sparse lexical list. The method has a small, explicit parameter vector: a dense prior $α\in [0,1]$, a score-versus-rank mix $λ\in [0,1]$, an RRF smoothing parameter $κ> 0$, optional list-geometry coefficients that move $α$ per query, and a router margin $τ$ that can turn sparse search off. We grid-search those ranges on SciFact train (809 queries) and freeze the chosen values on SciFact test (300). The tuned rank-score mix ($α= 0.8$, $λ= 0.75$, $κ= 20$) reaches 0.753 nDCG@10 and 0.889 recall@10, outperforming dense BGE (0.742 / 0.871) and equal-weight RRF (0.707 nDCG@10) on that test split. A list-conditioned $α$ adds +0.0006 nDCG; a sparse-off router is rejected by the same train split (any $τ$ that skipped approximately 50% of queries lost nDCG). These coefficients are dataset-specific. Equal RRF with the same models does not beat dense on a nine-zip BEIR macro-average (0.479 vs. 0.519 nDCG@10). Repeating the same train-then-freeze sweep independently on all 20 indexed units beats equal RRF on 20/20 and dense on 16/20 (unit-mean nDCG@10 0.467 vs. 0.462 dense vs. 0.420 RRF). Other corpora should reuse the ranges, not a copy of the SciFact point.